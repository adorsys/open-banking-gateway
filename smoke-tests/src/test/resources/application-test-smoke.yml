# Useless - just to make bean injection work
local:
  server:
    port: -1

test:
  smoke:
    opba:
      server-uri: https://obg-dev-openbankinggateway.cloud.adorsys.de
    aspsp-profile:
      server-uri: https://obg-testbank-aspspprofile.cloud.adorsys.de
  retry:
    max: 10
  webdriver:
    timeout: PT10S

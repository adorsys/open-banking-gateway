localapp:
  schema:
    # seem to be shared
    name: consent

spring:
  datasource:
    url: ${common.db.url}
    username: test-online-banking
    password: test-online-banking
    driver-class-name: ${common.db.driver}
  liquibase:
    enabled: true
server:
  port: ${common.apps.local.onlinebanking.port}
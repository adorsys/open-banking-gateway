# Validations are defined in format:
# input.<controlType>.<valueType>.<...> - 'input' is magic anchor to find code in message template
# I.e.: 'input.textbox.string.no.psu.id'
# where:
# controlType can be: textbox, dropdown, ...
# valueType can be: string, int, decimal, enum ...

input.textbox.ipaddr.no.psu.ip.ipaddress=PSU ip address is missing
input.textbox.string.no.psu.id=PSU bank login is missing
input.textbox.string.no.aspsp.id=Bank id missing
input.textbox.string.no.request.id=Request id missing
input.textbox.string.uri.redirect.ok=Redirect URI missing
input.textbox.string,uri.redirect.nok=Redirect NOK URI missing
